node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
test-results/
playwright-report/

# repository-owned background runtime metadata and logs
/.ai-note-runtime/

# internal build scaffolding — never published
BUILD_PLAN.md

# local agent working notes (plans, i18n drafts) — never published
/.hermes/

# local environment and provider credentials — never commit
.env*
!.env.example

# runtime meeting data (audio = PII, transcripts) — never commit
data/

# user's personal glossary (may hold private domain terms); ship glossary.example.json only
glossary.json

# local tool caches
.cache/

# audio artifacts (never commit real recordings)
*.webm
*.mp4
*.ogg
*.wav
*.m4a

# but DO commit tiny test fixtures
!fixtures/**
.DS_Store

# local dev-server launch config for the desktop app preview
/.claude/launch.json
